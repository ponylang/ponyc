# tools/lib

This directory contains libraries that are used as dependencies for tools that live in the tools directory.

## Vendored Dependencies

Unless otherwise noted, all subdirectories herein are "vendored" an as such, should not be modified. Any changes should be made to the upstream project and it should then be revendored.
