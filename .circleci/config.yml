version: 2

orbs:
    zulip: ponylang/zulip@dev:first

jobs:
  verify-changelog:
    docker:
      - image: ponylang/changelog-tool:release
    steps:
      - checkout
      - run: apt-get update
      - run: apt-get -y install curl
      - run: changelog-tool verify CHANGELOG.md
      - zulip/notify

  validate-shell-scripts:
    docker:
      - image: ponylang/ponyc-ci:shellcheck
    steps:
      - checkout
      - run: find . -name '*.bash' -exec shellcheck {} \;
      - zulip/notify

workflows:
  version: 2
  tests:
    jobs:
      # sanity tests
      - verify-changelog
      - validate-shell-scripts
