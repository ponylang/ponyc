name: "Stress Test: Linux Glibc Generative (Systematic)"

on:
  schedule:
    - cron: "0 14 * * *"
  workflow_dispatch:
    inputs:
      sha:
        description: 'SHA to test'
        required: true
        type: string
        default: 'main'

permissions:
  packages: write

# Second stress lane. The other stress workflows (the tcp-open-close trio) share
# the repo-wide `stress-scheduled` group (one at a time); the generative workflows
# -- systematic and normal -- share their own `stress-scheduled-generative` group,
# so at most one of each lane runs at once -- at most two stress workflows in
# total. Manual runs get a unique group (github.run_id) so a branch check never
# queues behind the loop. Relies on these workflows having exactly two triggers
# (schedule + workflow_dispatch) -- see the per-event flag/alert conditions below.
concurrency:
  group: stress-${{ github.event_name == 'workflow_dispatch' && github.run_id || 'scheduled-generative' }}
  cancel-in-progress: false

jobs:
  x86_64-linux-glibc:
    runs-on: ubuntu-latest
    # A bounded soak; the orchestrator also caps each seed with a 120s watchdog.
    # The 240m job cap is the shared generative ceiling -- see
    # .known-couplings/stress-test-concurrency.md.
    timeout-minutes: 240

    name: x86-64-unknown-linux-ubuntu26.04 generative systematic [debug]
    container:
      image: ghcr.io/ponylang/ponyc-ci-ubuntu26.04-builder:20260425
      options: --user pony

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ github.event.inputs.sha || 'main' }}
      - name: Restore prebuilt libs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LIBS_IMAGE: ghcr.io/ponylang/ponyc-ci-ubuntu26.04-builder:20260425
          LIBS_TAG: ${{ hashFiles('Makefile', 'CMakeLists.txt', 'lib/CMakeLists.txt', 'lib/llvm/patches/*') }}
          # Off main (a manual dispatch of a branch): build LLVM if it isn't cached,
          # and push what we build. On main (a scheduled run, or a dispatch left on
          # main): if it isn't cached, skip -- we don't build LLVM on main.
          LIBS_MODE: ${{ ((github.event_name == 'workflow_dispatch' && github.event.inputs.sha != 'main') && '--branch-cache') || '--require-cache-hit --skip-on-miss' }}
          LIBS_BUILD: ${{ ((github.event_name == 'workflow_dispatch' && github.event.inputs.sha != 'main') && '-- make libs llvm_tools=false build_flags=-j4') || '' }}
        run: python3 .ci-scripts/libs-cache/resolve_libs_cache.py $LIBS_MODE --image "$LIBS_IMAGE" --tag "$LIBS_TAG" $LIBS_BUILD
      - name: Build Debug Systematic Runtime
        if: ${{ hashFiles('.libs-cache-miss') == '' }}
        run: |
          make configure config=debug use=scheduler_scaling_pthreads,systematic_testing
          make build config=debug
      - name: Run Generative Stress Test
        if: ${{ hashFiles('.libs-cache-miss') == '' }}
        run: |
          python3 test/rt-stress/generative/orchestrate_systematic.py \
            --ponyc build/debug-scheduler_scaling_pthreads-systematic_testing/ponyc \
            --use-flags scheduler_scaling_pthreads,systematic_testing \
            --start ${{ github.run_id }} --count 250 \
            --out rt-stress-out
      # cancelled() too: keep bundles from seeds that ran before a cancel.
      - name: Upload failure bundles
        if: ${{ (failure() || cancelled()) && hashFiles('.libs-cache-miss') == '' }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: generative-systematic-bundles-linux-glibc
          path: rt-stress-out/*.json
          if-no-files-found: ignore
      # cancelled() too: failure() is false when a job hits timeout-minutes.
      - name: Send alert on failure or cancellation
        if: ${{ (failure() || cancelled()) && github.event_name == 'schedule' }}
        uses: zulip/github-actions-zulip/send-message@bd8ec52de371d139ae8313661b7d8318c19266aa
        with:
          api-key: ${{ secrets.ZULIP_SCHEDULED_JOB_FAILURE_API_KEY }}
          email: ${{ secrets.ZULIP_SCHEDULED_JOB_FAILURE_EMAIL }}
          organization-url: 'https://ponylang.zulipchat.com/'
          to: notifications
          type: stream
          topic: ${{ github.repository }} scheduled job failure
          content: ${{ github.server_url}}/${{ github.repository }}/actions/runs/${{ github.run_id }} failed.
