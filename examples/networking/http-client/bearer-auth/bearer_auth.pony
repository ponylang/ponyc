"""
Bearer token authentication example.

Connects to `httpbin.org` over HTTPS and sends a GET request with a Bearer
token in the Authorization header. Demonstrates the `Request` builder's
`.bearer_auth()` method for setting token-based authentication.
"""
