"""
Typed JSON API example.

Connects to `jsonplaceholder.typicode.com` over HTTPS, fetches a JSON todo
item, decodes the response into a typed `Todo` object using `JSONDecoder` and
`DecodeJSON`, and prints selected fields. Demonstrates typed JSON decoding and
`HTTPClientConnection.ssl()` for TLS connections.
"""
