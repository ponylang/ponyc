"""
Multipart form upload example.

Connects to `httpbin.org` over HTTPS and POSTs a multipart form with a text
field and a file attachment. Demonstrates `MultipartFormData` with
`Request.post().multipart_body()` for `multipart/form-data` uploads.
"""
