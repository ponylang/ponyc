"""
Query parameter encoding example.

Connects to `httpbin.org` over HTTPS and sends a GET request with
percent-encoded query parameters. Demonstrates the `Request` builder's
`.query()` method for appending RFC 3986 encoded parameters to the request
path.
"""
